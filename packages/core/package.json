{
  "name": "slip-auth-core",
  "version": "0.0.1",
  "description": "",
  "main": "src/core.ts",
  "scripts": {
    "format": "biome format --write",
    "test": "vitest run",
    "release": "pnpm run format && pnpm run test && pnpm publish && git push --follow-tags"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "slip-auth-database": "workspace:*",
    "db0": "^0.1.4",
    "uncrypto": "^0.1.3"
  },
  "devDependencies": {
    "@biomejs/biome": "1.8.3",
    "vitest": "^2.0.4"
  }
}
