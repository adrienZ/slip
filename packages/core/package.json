{
  "name": "slip-auth-core",
  "version": "0.0.5",
  "description": "",
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/core.mjs",
      "require": "./dist/core.mjs"
    }
  },
  "main": "./dist/core.mjs",
  "types": "./dist/core.d.ts",
  "files": ["dist"],
  "scripts": {
    "format": "biome format --write",
    "test": "vitest run",
    "build": "unbuild",
    "release": "pnpm run format && pnpm run test && pnpm run build && pnpm publish && git push --follow-tags"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "db0": "^0.1.4",
    "slip-auth-database": "workspace:*",
    "uncrypto": "^0.1.3"
  },
  "devDependencies": {
    "@biomejs/biome": "1.8.3",
    "unbuild": "^2.0.0",
    "vitest": "^2.0.4"
  }
}
